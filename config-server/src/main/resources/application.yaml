spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///D:/WORKSPACE/medical-config-repo
          default-label: main

server:
  port: 8888
